import React from 'react';
import ReactDOM from 'react-dom';
import EcommerceApp from './EcommerceApp';
import "./ecommerceApp.scss";

ReactDOM.render(
  <React.StrictMode>
    <EcommerceApp />
  </React.StrictMode>,
  document.getElementById('ecommerce-app')
);

